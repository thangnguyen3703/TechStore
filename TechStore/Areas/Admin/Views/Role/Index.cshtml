@{
    ViewData["Title"] = "Roles";
}

<div class="page-header">
    <div>
        <h1 class="page-title">Roles</h1>
        <p class="page-subtitle">Giao diện quản lý vai trò đã sẵn sàng để nối với service mới.</p>
    </div>
    <a asp-area="Admin" asp-controller="Role" asp-action="Create" class="btn-custom btn-custom-primary">
        <i class="bi bi-shield-plus"></i> New Role
    </a>
</div>
<div class="card border-0 shadow-sm">
    <div class="card-body py-5 text-center">
        <i class="bi bi-shield-lock display-5 text-muted"></i>
        <h2 class="h5 mt-3">Chưa có nguồn dữ liệu vai trò</h2>
        <p class="text-muted mb-0">Cần ViewModel và IRoleService của project mới trước khi hiển thị danh sách.</p>
    </div>
</div>
