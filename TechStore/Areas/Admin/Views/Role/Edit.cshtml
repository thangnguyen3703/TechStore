@{
    ViewData["Title"] = "Edit Role";
}

<div class="page-header">
    <div><h1 class="page-title">Edit Role</h1><p class="page-subtitle">Role ID: @(ViewData["RoleId"] ?? "chưa cung cấp")</p></div>
    <a asp-area="Admin" asp-controller="Role" asp-action="Index" class="btn-custom btn-custom-secondary">Back</a>
</div>
<div class="alert alert-info">Trang đang chờ RoleFormViewModel và IRoleService của project mới.</div>
