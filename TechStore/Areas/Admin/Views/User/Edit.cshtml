@{
    ViewData["Title"] = "Edit User";
}

<div class="page-header">
    <div><h1 class="page-title">Edit User</h1><p class="page-subtitle">User ID: @(ViewData["UserId"] ?? "chưa cung cấp")</p></div>
    <a asp-area="Admin" asp-controller="User" asp-action="Index" class="btn-custom btn-custom-secondary">Back</a>
</div>
<div class="alert alert-info">Trang đang chờ UserEditViewModel và IUserService của project mới.</div>
